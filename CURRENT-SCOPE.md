# 当前需求范围

更新日期：2026-09-23

## 1. 项目定位

本项目建设一套可复用的企业官网模板，包含官网前台、内容管理后台及基础部署能力，用于快速搭建不同企业的品牌展示、业务介绍、产品展示和咨询获客网站。

用户提供的四大业务板块及出海服务需求是本模板的参考场景，不代表模板只能服务该企业或行业。模板支持多种语言，品牌信息、业务内容、栏目、启用语言及联系方式应能够按具体企业配置。

本文件记录当前版本的目标范围，作为开发和验收依据；列入本文件不代表功能已经实现。后续新增或调整需求时，应同步更新本文件。具体技术设计在 `ARCHITECTURE.md` 中维护。

### 1.1 建设目标

- 对访客：快速了解企业、业务及产品，通过对应入口完成咨询、合作申请或应聘登记。
- 对运营人员：通过后台维护内容、翻译、媒体和展示顺序，日常更新无需修改页面代码。
- 对管理员：管理站点配置，控制发布操作，统一查看及导出三类留资。
- 对后续项目：复用页面模板、内容模型及后台能力，通过配置和替换素材形成不同企业的网站。

### 1.2 需求口径

| 标记 | 含义 | 开发与验收方式 |
| --- | --- | --- |
| 当前要求 | 本文件明确列出的功能和行为 | 按实际交付进行验证 |
| 示例 | 帮助理解业务，不固定为模板默认值 | 可替换为其他企业的数据 |
| 建议默认值 | 本次细化提出的字段或交互建议 | 实施前核对，可随项目调整 |
| 待确认 / 可选 | 尚需确定或按站点选择的内容 | 确定后更新文档，不视为已经承诺交付 |

以下字段表描述业务需求，不直接等同于数据库表结构。字段的长度、文件大小等数值应在实现前统一记录，不能仅在前端限制。

## 2. 当前技术约束与复用方式

- 沿用 `README.md` 确定的 Next.js + Go 技术方向，官网采用 SSR 架构，兼顾 SEO 和首屏展示。
- 项目分为 `web/` 官网、`admin/` 独立后台管理前端及 `server/` Go 服务；官网与后台独立开发和构建，共用服务端接口及权限规则，后台技术栈待初始化时确定。
- 当前以同一套模板代码为不同企业分别配置、部署为目标；各站点的数据和后台账号独立。
- 企业名称、Logo、品牌文案、业务名称、图片、联系方式等不得固定为示例企业内容。
- 支持基础品牌配置，如主色、Logo、站点名称；不承诺仅靠配置即可实现任意设计风格。
- 支持预设栏目及首页模块的启用、停用和排序；导航与栏目显示状态保持一致。
- “后台可配置”指在预设页面结构中维护内容、媒体、显示状态和顺序，不包含任意布局的拖拽建站器。
- 示例数据与实际企业数据分离，便于替换和初始化；示例不应包含真实用户留资信息。

### 2.1 站点配置分层

| 配置类型 | 主要内容 | 维护方式 |
| --- | --- | --- |
| 站点品牌 | 企业名称、站点名称、Logo、网站图标、主色 | 后台配置；名称及说明按语言维护，媒体允许复用 |
| 栏目结构 | 栏目名称、显示状态、顺序、导航链接 | 在预设栏目结构中配置，栏目名称支持翻译 |
| 业务内容 | 业务、产品、文章、岗位、企业介绍 | 使用对应模块维护，各语言独立保存及发布 |
| 公共信息 | 联系方式、地址、版权、备案信息 | 后台维护，可供页头、页脚和联系页面引用 |
| 语言配置 | 语言标识、显示名称、默认语言、状态、排序 | 后台管理，约束见第 4 节 |
| 部署配置 | 域名、数据库连接、文件路径、服务密钥 | 由部署环境维护，密钥不作为公开站点配置返回 |

### 2.2 模板复用流程

1. 为目标企业准备独立的部署环境、数据库及上传文件存储位置。
2. 初始化管理员和站点配置，填写品牌、域名和公共信息。
3. 配置启用栏目、默认语言及其他语言，调整首页模块和导航。
4. 替换示例内容、图片和基础界面翻译，补齐目标语言内容。
5. 预览并发布内容，验证页面、表单、权限及语言切换后上线。

验收时应能够用第二组企业资料完成一次配置替换，不依赖搜索代码并逐处修改企业文案。涉及全新页面结构的设计需求，应作为模板功能调整另行记录。

## 3. 官网前台

| 模块 | 当前功能范围 | 可配置内容与复用要求 |
| --- | --- | --- |
| 首页 | Banner、业务介绍、核心定位、服务链路、核心优势、合作模式、快速咨询入口 | 图片、文案、按钮及链接、模块显示状态和顺序；示例中的“全链路闭环”可替换为其他企业的服务流程 |
| 业务介绍 / 解决方案 | 业务板块详解、服务标准、适用客户、价值体现 | 板块名称、数量、内容、图片和排序；四大业务仅为示例，不固定数量 |
| 产品中心 | 产品列表与详情，展示标准化服务或产品、参考价格、服务 SLA | 产品内容、图片、价格说明、服务标准；价格和 SLA 可按站点需要隐藏，不要求所有行业都填写 |
| 关于我们 | 公司简介、使命愿景、企业实力 | 企业实力可展示仓储、渠道、团队，也可替换为其他行业所需内容 |
| 新闻资讯 | 新闻列表和详情，使用统一展示模板 | 分类名称与文章内容可配置；行业动态、公司动态、出海干货为示例分类 |
| 联系我们 | 快速咨询表单、企业联系方式和办公地址 | 表单说明、全球联系方式、地址、企业微信二维码、邮箱等，支持多条联系方式 |
| 招商加盟 / 合作 | 合作优势、合作模式、合作流程、申请表单 | 栏目名称及内容可配置，不需要招商功能的站点可关闭 |
| 人才招聘 | 公司文化、在招岗位、岗位详情、投递方式及应聘信息登记 | 内容、岗位及投递说明可配置，不需要招聘功能的站点可关闭 |
| 公共区域 | 页头导航、语言切换、页脚及咨询入口 | 导航、品牌信息、联系方式、版权和备案信息等 |

前台仅展示当前已发布且启用的内容。停用栏目或下架内容后，不应继续通过其详情地址向公众展示；具体跳转或未找到页面策略在实现时统一确定。

### 3.1 公共布局与导航

- 页头展示 Logo、启用的导航栏目、语言切换入口及主要咨询入口；移动端提供可展开和收起的导航。
- 导航字段包括显示名称、目标地址、打开方式、排序及显示状态。站内链接应跟随当前语言，外部链接应校验地址格式。
- 页脚展示可配置的企业信息、联系方式、栏目链接、版权及备案信息；未配置的可选信息不显示占位文案。
- 栏目关闭后，同步处理导航、首页入口、列表、详情及公开内容接口，避免通过旧链接继续访问。
- 列表支持分页，切换分类时回到第一页；没有数据时展示当前语言的空状态说明。
- 详情页应提供清晰的返回入口；失效地址展示统一的未找到页面或按已确定的策略跳转。

### 3.2 首页字段及行为

| 模块 | 主要字段 | 展示和交互要求 |
| --- | --- | --- |
| Banner | 标题、副标题、图片、移动端图片（可选）、按钮文字、按钮目标、排序、启用状态 | 支持配置多条；单条时不显示无意义的切换控件；按钮文字及目标成对填写 |
| 业务介绍 | 模块标题、简介、关联业务、排序 | 卡片展示业务名称、简介和图片，可进入对应详情 |
| 核心定位 | 标题、说明、配图 | 内容可替换为企业价值主张或品牌介绍 |
| 服务链路 | 模块标题、步骤名称、步骤说明、图标或图片、顺序 | 按配置顺序展示，步骤数量不固定 |
| 核心优势 | 标题、优势条目、说明、图片或图标 | 支持多个条目及排序 |
| 合作模式 | 标题、模式名称、说明、关联入口 | 可引导至合作页面或咨询表单 |
| 快速咨询 | 引导文案、按钮文字、跳转目标 | 指向咨询表单，并保留首页来源信息 |

首页模块可启停、排序；不需要的模块关闭后不保留空白区域。关联内容仅展示当前语言下可公开访问的版本，失效关联不得生成可点击的错误入口。

### 3.3 业务介绍 / 解决方案

- 列表条目：名称、简介、封面、顺序、展示状态、详情入口。
- 详情字段：业务定位、服务内容、服务标准、适用客户、价值体现、服务流程、合作方式及咨询入口。
- 服务流程等重复内容按条目维护，业务数量和条目数量不固定。
- 企业可以将栏目命名为“解决方案”“服务项目”等，不影响通用内容维护方式。
- 非必填区块未填写时不展示，必填字段在发布前校验。
- 咨询入口携带关联业务标识，访客无需重新选择已经明确的业务。

### 3.4 产品中心

| 字段组 | 主要字段 | 说明 |
| --- | --- | --- |
| 基础信息 | 产品名称、摘要、封面、详情图片、排序 | 名称和摘要可按语言分别维护 |
| 产品介绍 | 正文、服务范围、适用客户、服务流程 | 兼容实物产品介绍及标准化服务介绍 |
| 参考价格 | 是否展示、价格数值或说明、币种、计价单位 | 可表达固定参考价、起步价或面议；不按语言自动换汇 |
| 服务标准 | SLA、交付内容、补充说明 | 可按企业业务需要选择展示 |
| 转化入口 | 咨询按钮文字、关联产品 | 提交咨询时记录产品关联关系 |

列表至少展示名称、摘要和封面，可显示已配置的价格说明。详情展示当前发布内容；价格与 SLA 隐藏时不得残留字段标题。产品分类或筛选维度如需启用，先确认分类结构和查询规则。

### 3.5 关于我们

- 公司简介：企业名称、概述、详细介绍及配图。
- 使命愿景：标题及内容，可拆分展示或按设计组合。
- 企业实力：可配置多个板块，每个板块包含名称、说明及图片，例如仓储、渠道、团队、研发或生产能力。
- 图片及文案按实际企业替换；企业资质、发展历程等额外区块是否需要，在首个项目设计时确认。

### 3.6 新闻资讯

- 分类字段：名称、说明、排序及显示状态；分类名称支持多语言。
- 文章字段：标题、摘要、封面、分类、正文、展示日期、排序和发布状态。
- 列表展示标题、摘要、封面和日期，支持按分类查询及分页。
- 详情展示标题、日期和正文；正文支持基本标题、段落、列表、图片和链接。
- 展示日期与系统记录的实际发布时间分别处理；改变展示日期不等于设置定时发布。
- 排序建议默认采用“人工排序优先，其次展示日期倒序，最后稳定标识排序”，确保翻页结果稳定。
- 分类被文章引用时，删除或关闭前应提示影响并明确处理关联文章，不允许形成无法维护的分类引用。

### 3.7 联系我们、合作及招聘

| 页面 | 内容字段 | 表单及交互 |
| --- | --- | --- |
| 联系我们 | 页面说明、联系人或部门、电话、邮箱、国家或地区、办公地址、二维码、地址跳转链接 | 展示快速咨询表单；电话、邮箱和链接使用相应交互 |
| 招商加盟 / 合作 | 合作优势、合作模式、合作条件说明、流程步骤、申请引导 | 展示合作申请表单，关闭栏目后不再接受该栏目表单提交 |
| 人才招聘 | 公司文化、岗位名称、工作地点、岗位职责、任职要求、投递方式 | 展示岗位列表和详情，登记时绑定岗位；岗位停止招聘后不再接受该岗位申请 |

联系方式支持多条，并按设定顺序展示。地图采用可配置的地址跳转链接；如需嵌入地图服务，应另行明确服务商及接入方式。

### 3.8 响应式与视觉交付

- 适配手机、平板和 PC，确保导航、列表、详情和表单在不同屏幕上可正常使用。
- 兼顾长文本和不同语言的排版，避免西班牙语等较长文本导致布局溢出。
- 参考项目包含设计方案两轮集中改稿；这是单个项目的交付约定，模板复用到其他项目时另行确定。
- 建议验收宽度包括 375px、768px 和 1440px，并检查中间宽度的连续缩放效果。
- 图片保留合理比例，正文长链接、长单词及表格不得造成整页横向溢出。
- 表单字段具备可识别的标签、错误提示和键盘操作能力；按钮与导航应有可见的焦点状态。
- 图片支持替代文本，品牌主色配置应保持正文和按钮的可读性。

### 3.9 基础 SEO 与页面状态

结合 README 中的 SSR 和 SEO 目标，页面需具备以下基础能力：

- 标题、主要正文和基本页面描述可通过服务端渲染获得。
- 页面支持按语言维护 SEO 标题和描述；未单独填写时使用已发布的标题及摘要作为默认值。
- 内容地址应稳定，各语言使用可区分的地址；具体路由格式由架构文档确定。
- 根据已发布且可访问的页面生成站点地图，多语言对应关系只包含实际可访问的页面。
- 后台和草稿预览不参与公开收录，预览仍必须通过身份及权限校验。
- 页面应具备加载、空数据、提交中、失败和未找到等必要状态，提示文案使用当前语言。

## 4. 多语言

- 支持同一站点同时启用多种语言，语言数量和组合不限定为双语；中文、英文、西班牙语等均可按项目需要配置。
- 后台支持添加语言、启用或停用语言、设置默认语言和展示顺序；前台语言切换入口根据启用语言动态生成。
- 内容结构按语言标识组织，新增语言无需增加一套固定语言字段或复制页面代码。
- 业务、产品、新闻、公共文案和导航等面向访客的内容支持对应语言维护。
- 翻译由运营人员自行完成并在后台录入，本期不包含机器自动翻译或人工翻译服务。
- 各语言内容的录入和发布不应覆盖其他语言内容。
- 语言切换优先定位到对应内容；目标语言尚未发布时的处理策略需统一明确，不能无提示地混合展示不同语言。
- 前台按钮、表单提示和校验信息也应支持已启用语言；新增语言需要补齐这些基础翻译。
- 多语言以官网前台为范围，后台界面多语言不作为本期要求。

### 4.1 语言配置字段

| 字段 | 规则 |
| --- | --- |
| 语言标识 | 站点内唯一，例如 zh-CN、en、es；作为翻译内容的稳定关联依据 |
| 显示名称 | 可使用语言本身的名称，供访客在语言切换入口识别 |
| 默认语言 | 必须有且只有一个启用的默认语言 |
| 启用状态 | 只有启用语言参与前台展示；停用不删除已有翻译 |
| 展示顺序 | 用于前台语言切换入口排序 |
| 基础界面翻译 | 按钮、导航、分页、表单标签、校验提示、成功及失败提示 |

- 添加语言后，可先处于未启用状态并补齐翻译；启用前检查必需的界面文案和首页内容。
- 未启用语言允许录入、预览和生成发布记录，但不对公众展示；内容准备完成后再启用语言，避免启用与首次发布相互依赖。
- 停用默认语言前，必须先选择其他启用语言作为默认语言，不能让站点处于无默认语言状态。
- 已有关联内容的语言标识应保持稳定；需要更换标识时另行处理内容及地址映射。
- 后台应能维护基础界面翻译，满足新增语言无需修改页面代码的要求。
- 多语言扩展应考虑字体、日期和数字显示；如首个项目需要从右向左书写的语言，需同时确认布局方向及对应验收用例。

### 4.2 内容关联与独立发布

- 同一业务、产品、文章或岗位使用稳定的内容标识关联多种翻译，避免将翻译误建为无关条目。
- 图片、排序等非文本属性可复用；不同语言如需单独配图，应支持语言级覆盖。
- 标题、摘要、正文、图片替代文本及 SEO 信息按语言分别维护。
- 后台能够区分某语言的未录入、草稿、已发布及有未发布修改状态。
- 发布操作明确标注目标语言；发布某一语言不得隐式发布其他语言的草稿。
- 全局栏目关闭影响所有语言；单个语言版本下架仅影响该语言版本。

### 4.3 缺失翻译与语言切换

- 目标内容存在已发布译文时，切换到同一内容的目标语言地址。
- 尚未发布译文时，不把其他语言正文当作已翻译内容展示。
- 建议默认行为：提示目标语言暂不可用，并提供目标语言栏目页或首页入口；最终策略在上线前确认。
- 停用语言不再出现在切换入口，其旧地址的返回或跳转行为应与整体路由策略一致。
- 访客主动选择语言后，站内导航及后续页面保持该语言；首次访问的默认语言选择策略待项目确认。

## 5. 管理后台

### 5.1 账号与角色

采用两级角色，至少落实以下权限边界：

| 能力 | 管理员 | 内容编辑 |
| --- | --- | --- |
| 后台账号信息查看 | 可查看账号列表及基础信息，不展示密码 | 不开放账号列表，个人基础信息除外 |
| 官网内容、产品、新闻维护 | 可使用 | 可使用授权的内容维护功能 |
| 留资查看、查询及导出 | 可使用 | 不可使用 |
| 内容发布与上下架 | 可使用 | 是否允许直接操作待确认 |

账号如何初始化、是否需要在后台新增账号、停用账号及重置密码，待进一步确定；当前需求不包含公众注册。

### 5.2 内容管理范围

- 首页及公共配置：维护品牌信息、首页模块、导航、页脚和联系方式。
- 业务与公司内容：查看和编辑业务介绍、关于我们、合作说明、公司文化等预设页面内容。
- 产品中心：查看和编辑产品内容，维护其列表展示及详情信息。
- 新闻管理：新增、删除、修改、查询、上下架和排序。
- 招聘管理：维护在招岗位、岗位内容及投递方式。
- 多语言内容：按启用语言录入及维护相应字段。
- 支持上述内容需要的图片上传及引用；独立的高级素材管理系统不在本期范围内。

产品、业务板块和岗位的新增、移除及上下架操作细节，应在字段及页面设计时明确，以满足不同企业的内容配置需要。

### 5.3 后台页面与操作细节

| 页面类型 | 主要能力 | 反馈要求 |
| --- | --- | --- |
| 内容列表 | 标题查询、语言与状态筛选、分页、查看详情、进入编辑 | 显示当前状态、更新时间及可执行操作 |
| 内容编辑 | 分语言填写字段、上传图片、保存草稿、查看当前发布内容 | 必填和格式错误定位到字段；保存失败保留已输入内容 |
| 内容预览 | 预览当前草稿的页面效果 | 标明草稿和语言，仅授权后台用户可访问 |
| 发布与上下架 | 对指定内容和语言执行操作 | 显示操作对象、影响范围及结果，避免误发布其他语言 |
| 排序和显示配置 | 调整顺序、启停栏目或模块 | 排序结果稳定；草稿配置提交发布后再对前台生效 |
| 账号查看 | 查看账号标识、显示名称、角色、状态等基础信息 | 仅管理员可访问，不返回密码或认证密钥 |

- 有未保存修改时离开编辑页应给予提示。
- 两个用户先后编辑同一草稿时，保存前检测内容是否已被他人更新，提示冲突，避免静默覆盖。
- 可发布的品牌、首页、导航及公共文案配置也采用草稿和发布隔离；账号权限等管理设置按操作立即生效。
- 管理员维护语言启停、默认语言等站点级设置；内容编辑维护翻译内容。站点级设置发生公开影响时应显示提示。
- 富文本仅允许已支持的格式，展示前过滤不安全内容，避免脚本或不受控嵌入内容随文章执行。
- 上传图片应检查类型、文件内容及大小，上传失败说明原因；正在被生效页面或发布记录引用的文件不能直接移除。
- 为内容保存、发布、下架、删除及导出保留必要的操作人、操作时间和对象标识，便于定位问题；记录中不写入密码或完整留资正文。

### 5.4 统一内容发布规则

“保留原记录、不覆盖已发布内容”按以下规则落实，适用于新闻及其他可发布内容：

1. 新建或编辑内容先保存为草稿；编辑草稿不改变官网当前显示的内容。
2. 发布时生成新的发布记录，并将其设置为当前生效记录；先前发布记录保留，不原地覆盖。
3. 官网读取当前生效的发布记录，未发布草稿不得通过公开接口暴露。
4. 下架后内容不再公开展示，已保留的发布记录不因下架而丢失。
5. 新闻删除采用逻辑删除，保留已有记录；其他内容的移除操作也不得破坏需保留的发布记录。
6. 本期不提供历史版本对比、一键回滚或恢复任意历史版本的界面。

记录保留用于满足原始需求，不等于交付完整版本管理系统。具体数据结构在架构设计中确定。

| 业务状态 | 后台表现 | 官网表现 |
| --- | --- | --- |
| 新建草稿 | 可继续编辑、预览、发布 | 无公开内容 |
| 首次发布 | 生成发布记录并设置为生效记录 | 展示该语言的发布内容 |
| 已发布后继续编辑 | 同时存在当前发布内容和未发布修改 | 继续展示原发布内容 |
| 再次发布 | 生成新记录，切换当前生效记录 | 展示新发布内容 |
| 下架 | 保留内容及发布记录，可继续维护 | 列表、详情和公开接口均不展示 |
| 逻辑删除 | 标记删除，常规列表不再展示 | 不可公开访问 |

- 发布需校验必填字段、语言配置及内容关联，失败时原生效记录保持不变；语言启用状态控制公开访问，不阻止后台准备发布内容。
- 多次点击发布按钮不应产生意外的重复发布；保存、发布和生效指向更新必须保证数据一致。
- 发布成功后应刷新相应页面的缓存；下架或栏目关闭同样需要处理列表、详情及关联入口缓存。
- 缓存刷新失败时应提供明确反馈及重试方式，不能在页面仍展示旧内容时无条件提示全部生效。
- 保留的旧记录可以用于数据追溯；其访问权限不能因已曾经发布而变为公开可读。

## 6. 表单与留资

| 留资类型 | 前台来源 | 后台要求 |
| --- | --- | --- |
| 快速咨询 | 首页咨询入口、联系我们及业务/产品咨询入口 | 独立查询和详情查看 |
| 合作申请 | 招商加盟 / 合作页面 | 独立查询和详情查看 |
| 应聘登记 | 招聘页面或岗位详情 | 独立查询和详情查看 |

- 三类留资分别管理，仅管理员可查看、查询和导出；权限限制同时落实到后台页面和服务端接口。
- 至少支持按提交时间查询，并提供 Excel 导出能力。
- 保存留资类型、来源页面、提交时间以及对应表单数据，便于辨别咨询来源。
- 表单字段、必填规则和提示文案在实施前确定；一期使用预设表单，不包含任意字段的可视化表单设计器。
- 表单应具备必要的输入校验、防重复提交和基础频率限制。
- 招聘默认包含应聘信息登记及投递方式展示；简历附件上传尚未确认为本期必做功能。
- 如需隐私说明或确认提示，应支持在页面配置对应文案和链接，具体内容由站点运营方提供。

### 6.1 建议表单字段

以下为建议默认字段，实际必填组合在实施前确定，并按站点使用场景调整。

| 字段 | 快速咨询 | 合作申请 | 应聘登记 |
| --- | --- | --- | --- |
| 姓名 / 联系人 | 必填 | 必填 | 必填 |
| 手机或邮箱 | 至少填写一种 | 至少填写一种 | 至少填写一种 |
| 公司名称 | 可选 | 建议必填 | 不展示 |
| 国家或地区 | 可选 | 建议必填 | 可选 |
| 关联业务 / 产品 | 从来源入口带入，允许选择 | 按合作场景配置 | 不展示 |
| 合作意向及资源说明 | 不展示 | 建议必填 | 不展示 |
| 应聘岗位 | 不展示 | 不展示 | 从岗位入口带入并校验 |
| 需求描述 / 补充说明 | 需求描述 | 合作说明 | 个人介绍或补充说明 |
| 隐私确认 | 启用相关说明时按配置展示 | 同左 | 同左 |

系统同时记录提交编号、类型、来源页面、页面语言、关联内容标识及提交时间。手机号校验应兼容国际联系方式，不固定使用中国大陆手机号格式；来源参数不得直接作为可信内容执行或展示。

### 6.2 提交流程与异常处理

1. 用户填写后，前端展示必填及格式提示；服务端再次校验相同业务规则。
2. 提交期间显示处理中状态并限制重复点击，网络重试不得意外创建重复记录。
3. 成功保存后显示当前语言的成功提示；失败时保留用户输入并提供重试入口。
4. 服务端确认对应栏目、岗位或关联内容仍可接收申请，避免页面打开后内容已下架仍继续入库。
5. 对频繁提交实施限制并返回可理解的提示；是否引入第三方验证码及其服务商待确认。

### 6.3 后台查询和 Excel 导出

- 三类留资分别提供列表与详情，列表显示提交时间、联系人、联系方式及对应的主要业务字段。
- 查询至少支持时间范围；联系人或联系方式关键词查询作为建议默认能力，最终查询字段在实施时确定。
- 时间范围的筛选口径与页面展示时区保持一致；导出注明使用的时区。
- 导出当前筛选条件匹配的数据，不默认为仅导出当前分页；导出前明确类型和预计条数。
- Excel 文件包含清晰的列名、提交编号和提交时间，字段与所选留资类型对应。
- 手机号等字段按文本保存，保留国际区号及前导零；用户输入不得被解释为电子表格公式。
- 导出文件及下载接口同样需要管理员权限，不能使用长期公开地址暴露数据。
- 数据量较大时的单次导出上限或分批规则在实施时明确，达到限制时说明处理方式。
- 内部备注、处理状态、留资删除及保存期限仍需确认后再纳入具体交付。

## 7. 基础设施与部署

| 项目 | 当前范围 | 可选项与费用边界 |
| --- | --- | --- |
| 服务器 | 提供部署及运行环境搭建方案；参考配置为阿里云 ECS 2C4G | 云厂商、地区和规格可按项目调整；租赁费另计，容量需结合实际负载验证 |
| 域名与备案 | 支持自定义域名；参考交付包含 .com/.cn 注册和备案材料提交协助 | 域名费另计；域名持有人、材料及部署地区由具体项目确定，适用要求在上线前核实 |
| HTTPS | SSL 证书配置 | 证书获取和续期方式在部署方案中明确 |
| Cloudflare | 可选的海外 CDN 与基础 DDoS 防护接入 | 参考使用免费版，具体权益以接入时服务说明为准；不作为所有站点必选依赖 |
| 数据库 | MySQL 数据设计与部署，参考方式为 ECS 本地部署 | 不限制后续迁移到独立数据库；迁移不属于本期交付 |
| 备份 | 提供手动备份与恢复步骤；支持将定时脚本或快照列为可选部署项 | 原始需求允许初期手动备份，自动备份不默认计入必做范围 |

备份范围应覆盖数据库中的网站配置、内容、账号、发布记录和留资，以及站点使用的上传文件。Excel 留资导出只覆盖业务数据的一部分，不能替代完整备份。

### 7.1 部署配置与运行要求

- 提供环境变量说明，明确公开站点地址、后台地址、数据库连接、上传目录和必要密钥的配置方法。
- 凭据不写入代码仓库；对外展示的配置不得包含数据库密码、会话密钥等信息。
- HTTPS 部署包含访问跳转及证书续期说明，续期方式以实际部署方案为准。
- 后台需登录后访问，登录失败提供统一提示；认证状态过期后要求重新登录。
- 数据库不直接暴露给公众访问，公开接口只能返回已发布内容和必要字段。
- 表单提交、后台接口及草稿预览不被公共 CDN 缓存；静态资源和公开内容的缓存规则分别配置。
- 提供启动、停止、更新、日志查看及故障定位步骤；2C4G 是部署参考，性能以实际运行结果验证。

### 7.2 备份与恢复交付

- 明确备份对象、执行方式、保存位置、保留时间及负责人员。
- 手动备份提供可重复执行的操作步骤；选用自动备份时记录执行周期、结果检查方式及失败处理办法。
- 上传文件与数据库应保持可恢复的对应关系，保留正在使用的历史发布资源。
- 备份文件和导出文件均限制访问；建议至少保存一份服务器之外的备份副本，具体位置由项目确定。
- 上线前在独立测试环境完成一次恢复验证，检查已发布内容、图片及测试留资是否完整；不覆盖生产数据进行演练。

### 7.3 交付资料

- 官网、后台和服务端源代码及对应依赖说明。
- 数据库初始化或迁移方式、示例内容初始化方式和管理员初始化说明。
- 站点配置、栏目管理、多语言录入、发布、留资查询及导出的操作说明。
- 部署、更新、HTTPS、备份与恢复说明。
- 实际启用功能及待确认事项的落实记录、验收记录和已知问题清单。

## 8. 验收标准

- 替换站点配置及内容后，可用于另一家企业官网；页面和代码不依赖示例企业名称、固定四大业务或特定联系方式。
- 启用的预设栏目可以正常访问；关闭的栏目不会继续出现在导航和公开内容中。
- 官网展示内容可通过约定的后台字段维护，保存草稿不会立即改变线上页面。
- 发布后展示新内容，旧发布记录仍保留；上下架及删除行为符合统一规则。
- 使用至少三种语言验证内容录入、独立发布、前台展示及语言切换，各语言内容互不覆盖；基础界面文案与当前语言一致。
- 后台可添加和启停语言、设置默认语言和展示顺序；新增语言并补齐翻译后，无需修改页面代码即可展示。
- 快速咨询、合作申请和应聘登记能够正常提交、分别查询并导出 Excel。
- 内容编辑无法通过页面或接口读取、查询、导出留资信息。
- 手机、平板、PC 的主要页面及表单无明显溢出、错位或无法操作的问题。
- 部署环境能够正常访问，HTTPS 有效，并具备实际采用的备份方式及恢复说明。
- 验收记录注明启用栏目、启用语言、部署选项及尚未启用的可选功能。

### 8.1 核心验收场景

| 编号 | 验证操作 | 预期结果 |
| --- | --- | --- |
| AC-01 | 将企业 A 的品牌、联系方式和业务内容替换为企业 B | 官网展示企业 B 信息，主要业务页面无需修改代码 |
| AC-02 | 调整业务数量、首页模块顺序并关闭合作栏目 | 顺序和数量符合配置，合作入口和公开页面同步关闭 |
| AC-03 | 添加第三种语言，补齐文案、发布并启用 | 可通过前台切换访问，界面和正文使用对应语言 |
| AC-04 | 修改某一语言的已发布文章但只保存草稿 | 官网仍展示旧内容，其他语言不受影响 |
| AC-05 | 发布上述草稿 | 官网切换到新内容，数据库保留原发布记录 |
| AC-06 | 下架文章并访问旧地址、列表和公开接口 | 均不再展示下架内容，后台仍保留记录 |
| AC-07 | 切换至没有对应译文的语言 | 按确认的缺失翻译策略展示，不无提示混入其他语言正文 |
| AC-08 | 尝试停用当前默认语言 | 要求先设置另一个启用语言为默认语言 |
| AC-09 | 分别提交咨询、合作申请和岗位申请 | 数据进入对应模块，来源、语言及关联对象记录正确 |
| AC-10 | 表单重复点击、发送非法字段或对已关闭岗位提交 | 不产生意外重复记录，非法及失效提交得到明确拒绝 |
| AC-11 | 内容编辑直接请求留资详情或导出接口 | 服务端拒绝，响应不包含留资数据或下载地址 |
| AC-12 | 管理员按时间筛选并导出含国际手机号的数据 | 导出覆盖筛选结果，手机号、时间及文本格式正确 |
| AC-13 | 同时打开两个编辑会话，依次保存同一内容 | 后保存者得到冲突提示，不静默覆盖他人修改 |
| AC-14 | 在手机、平板及 PC 查看长文本、图片和表单 | 页面可读且可操作，无整页横向溢出 |
| AC-15 | 检查公开页面、站点地图与草稿预览 | 已发布正文可服务端渲染，草稿受认证保护且不进入站点地图 |
| AC-16 | 按交付步骤在测试环境恢复备份 | 页面、上传文件、发布内容和测试留资对应完整 |

### 8.2 验收记录要求

- 记录测试环境、版本、浏览器、视口宽度、启用语言和所用测试数据。
- 区分通过、未通过和未启用的可选项，失败项记录复现步骤和处理结果。
- 至少覆盖一次完整流程：配置站点、录入多语言内容、预览发布、访客提交、管理员查询并导出。
- 使用虚构联系人及联系方式完成测试，测试留资与实际运营留资可辨别。

## 9. 实施前待确认事项

- 首个示例站点的品牌素材、业务名称、实际产品及初始内容数量。
- 页面字段和富文本边界、首页模块组合、基础主题配置范围。
- 内容编辑能否直接发布与上下架，以及后台账号维护的具体操作范围。
- 三类表单的字段、必填项、是否需要简历附件，以及留资保留和清理规则。
- 首个站点启用的语言、默认语言、目标语言内容缺失时的展示策略，以及内容发布前必须具备的字段。
- 部署地区、域名、Cloudflare 是否启用，以及备份方式和保留周期。
- 示例项目的内容录入、翻译、设计修改、上线协助和维护服务分工。

### 9.1 待确认事项的处理顺序

| 阶段 | 需要落实的事项 | 影响 |
| --- | --- | --- |
| 页面与字段设计前 | 栏目组合、必填字段、表单字段、产品分类和招聘投递方式 | 决定后台表单、页面结构和数据关系 |
| 权限与发布开发前 | 编辑发布权限、账号维护范围、共享属性与语言级属性的发布方式 | 决定操作入口和服务端校验 |
| 多语言实现前 | 启用语言、默认语言、缺失翻译策略、首次访问语言选择、是否涉及从右向左布局 | 决定路由、切换行为和排版要求 |
| 部署前 | 域名及地区、服务规格、缓存、上传限制、备份周期与访问方式 | 决定运行配置及交付说明 |
| 验收前 | 初始化内容、翻译完整性、导出规模、内容生效时限及维护分工 | 决定最终测试数据和验收口径 |

每项确认结果应更新到对应章节；遇到新的业务模块、第三方服务或额外工作流时，先记录其目标、字段、操作和验收方式，再纳入当前范围。

## 10. 范围变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-23 | 根据示例需求建立初始范围，明确以可复用企业官网模板为目标，将企业内容、栏目及语言配置与通用能力分开，并标明可选项与待确认事项。 |
| 2026-09-23 | 明确支持多种语言及可扩展的语言配置，补充多语言验收标准；删除“本期不包含”章节并调整后续编号。 |
| 2026-09-23 | 细化页面字段与交互、站点复用方式、多语言配置、后台操作、发布状态、表单及导出、部署交付和可执行验收场景；建议默认值与待确认事项单独标明。 |
| 2026-09-23 | 明确新增的 `admin/` 专门承载后台管理，官网、后台和 Go 服务按三个目录独立组织。 |
