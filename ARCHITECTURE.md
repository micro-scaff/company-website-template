# 系统架构

更新日期：2026-09-23

## 1. 文档定位与实现状态

本文件依据 [CURRENT-SCOPE.md](CURRENT-SCOPE.md) 定义可复用企业官网模板的前端、后端、数据及部署架构，用于指导后续实现和技术评审。功能及验收范围以需求文档为准，本文描述如何实现这些需求。

截至本次检查，仓库状态如下：

| 部分 | 已有内容 | 实现状态 |
| --- | --- | --- |
| `web/` | Next.js App Router 初始化项目、TypeScript、Tailwind CSS、ESLint | 仍为初始化页面，负责官网及受保护的官网草稿预览 |
| `web/package.json` | Next.js 16.3.6、React / React DOM 19.2.8，Tailwind CSS 4、TypeScript 5 | 版本来自仓库声明，不代表运行和部署已验证 |
| `admin/` | 后台管理系统说明文档 | 独立管理端，尚未初始化框架、依赖和业务页面 |
| `server/` | Go 后端服务说明文档 | Go 工程、接口和数据库尚未初始化；说明中的数据库与缓存描述不代表已经集成 |
| 需求文档 | 官网、后台、多语言、内容发布和留资范围 | 已记录目标需求，业务待确认项继续保留 |

下文的目录、表名、接口和流程是本项目的目标设计，不表示已经存在对应实现。具体依赖版本在初始化或升级时锁定；实现 Next.js 相关功能前，应按 `web/AGENTS.md` 阅读所安装版本的本地文档。

## 2. 总体架构与技术选择

采用单站点独立部署的架构，代码分为 `web/` 官网、`admin/` 管理后台和 `server/` Go 服务三部分。官网与管理后台独立开发和构建，共用模块化单体 Go 服务及 MySQL 数据库。不同企业分别部署同一套代码，使用各自的配置、数据库、媒体目录及密钥。

| 层级 | 选择 | 职责及理由 |
| --- | --- | --- |
| 官网前端 | Next.js App Router + React + TypeScript + next-intl | 服务端渲染正文，处理路由、页面组合、多语言展示和 SEO |
| 管理后台 | 独立 `admin/` 前端工程，框架与组件库待选 | 承载账号、内容、多语言及留资管理；默认按客户端应用设计，不承担官网 SEO |
| 样式 | 官网沿用 Tailwind CSS + CSS 变量；后台独立确定 | 官网变量承载品牌主题，后台按管理界面需求选择组件和样式 |
| 数据请求 | 各前端工程独立封装请求客户端 | 分离服务端与浏览器请求，通过共同接口契约统一数据和错误格式 |
| 业务服务 | Go + 标准库 `net/http` | 按业务模块划分路由和服务，初期保持一个可部署进程 |
| 数据访问 | `database/sql` + MySQL 驱动 | 显式管理 SQL、连接池及发布事务；驱动版本在初始化时锁定 |
| 数据库 | MySQL，InnoDB，utf8mb4 | 保存关系数据、版本快照和必要的结构化 JSON |
| 文件存储 | 初期本地持久化目录，封装存储接口 | 满足单机部署，后续可替换为对象存储 |
| 访问入口 | Nginx + HTTPS | 统一域名、反向代理、请求大小控制及静态资源分发 |
| 部署 | Docker Compose 作为目标部署方式 | 管理 Next.js、Go、MySQL 和 Nginx；后台独立构建产物由 Nginx 托管 |
| 海外加速 | 按项目选择 Cloudflare | 初期仅考虑适合缓存的静态资源，业务正确性不依赖 CDN |

Go、Node.js 和 MySQL 的具体运行版本在部署实现时确定并锁定，不使用浮动的 `latest` 镜像作为交付依据。富文本编辑器、Excel 库和测试工具在实现对应模块时选择，避免将未安装依赖描述为已使用。

### 2.1 请求流向

| 请求来源 | 路径 | 处理链路 |
| --- | --- | --- |
| 官网访客 | `/{locale}/...` | 浏览器 → Nginx → Next.js → Go 公开查询接口 → MySQL |
| 后台用户 | `/admin/...` | 浏览器 → Nginx → admin 构建产物 → 浏览器渲染管理界面 |
| 后台数据操作 | `/api/v1/admin/...` | 浏览器 → Nginx → Go 鉴权、业务服务 → MySQL / 文件存储 |
| 访客表单 | `/api/v1/public/leads/...` | 浏览器 → Nginx → Go 校验及入库 → MySQL |
| 公开媒体 | `/media/{assetId}` | 浏览器 → Nginx → Go 校验可公开引用关系 → 文件存储 |
| 草稿预览 | `/preview/{id}/{locale}` | admin 打开预览 → Nginx → web 的 Next.js → Go 管理接口鉴权 → 草稿数据 |

Next.js 服务端通过内部地址访问 Go；两个前端的浏览器请求统一使用同源 `/api/v1`。Nginx 将 `/api/v1/` 和 `/media/` 转发至 Go，将 `/admin/` 交给独立后台产物，官网与 `/preview/` 请求转发至 Next.js。Go 和数据库端口只开放给内部服务。

目录独立不要求域名独立，初期保留同一域名下的 `/admin` 访问方式，便于会话和预览协作。后续如改用后台独立域名，必须同步设计主机级 cookie、预览授权和允许来源，不能直接沿用同源假设。

### 2.2 服务职责边界

- `web` 负责官网呈现、交互、路由、元信息及受保护的页面预览，不直接连接 MySQL。
- `admin` 负责管理页面、编辑表单、操作反馈和下载，通过 Go 管理接口访问业务数据，不直接连接数据库。
- Go 是业务规则及授权判断的唯一执行入口，负责最终校验、发布、留资、上传和导出。
- 前端隐藏按钮只改善操作体验，服务端必须独立验证操作权限。
- Next.js 的 Route Handler 或 Server Action 如被使用，只承担必要的页面适配或请求转发，不复制 Go 业务逻辑。
- 同一内容的公开数据、编辑数据和预览数据使用不同 DTO，避免将数据库整行或草稿字段直接返回访客。
- 多语言、业务模块及品牌配置从数据读取，模板代码不写入示例企业的业务名称或联系方式。
- 两个前端通过 API 契约协作，不互相导入应用目录或依赖彼此的运行时；确有共享代码需求时再抽取独立包。

## 3. 目标目录结构

以下目录逐步按功能建立，无需一次性生成空模块。

```text
company-website-template/
├── web/
│   ├── app/
│   │   ├── (entry)/
│   │   │   ├── layout.tsx             # 无语言入口的最小根布局
│   │   │   └── page.tsx               # 根地址选择语言并跳转
│   │   ├── (site)/
│   │   │   └── [locale]/
│   │   │       ├── layout.tsx         # 官网根布局，从路由设置语言
│   │   │       └── ...                # 官网各页面
│   │   ├── (preview)/
│   │   │   └── preview/[id]/[locale]/
│   │   │       ├── layout.tsx         # 受保护预览根布局
│   │   │       └── page.tsx           # 复用官网模板，不承载管理功能
│   │   ├── robots.ts
│   │   ├── sitemap.ts
│   │   └── globals.css
│   ├── components/
│   │   ├── ui/                       # 官网基础组件
│   │   └── site/                     # 官网布局和页面模块
│   ├── features/                     # 语言切换、咨询及申请交互
│   ├── lib/
│   │   ├── api/                      # server、browser、errors
│   │   ├── i18n/                     # 语言解析、字典、URL 和格式化
│   │   └── validation/               # 交互校验及共享约束
│   ├── types/                        # API DTO，优先从契约生成
│   └── tests/
├── admin/
│   ├── README.md
│   ├── src/                          # 逻辑划分示意，按选定框架落地
│   │   ├── pages/                    # 登录、站点、内容、语言、留资、账号
│   │   ├── layouts/                  # 管理布局与登录布局
│   │   ├── components/               # 表格、编辑器、上传及操作反馈
│   │   ├── features/                 # 各管理模块的交互与状态
│   │   ├── api/                      # Go 管理接口请求封装
│   │   ├── auth/                     # 当前用户、能力、路由守卫
│   │   ├── types/                    # 从共同契约生成或核对的类型
│   │   └── router/                   # /admin 基础路径及页面路由
│   ├── public/
│   ├── tests/
│   └── package.json                  # 初始化技术栈时创建
├── server/
│   ├── cmd/api/main.go               # 服务启动与依赖装配
│   ├── cmd/manage/main.go            # 初始化账号及维护命令
│   ├── internal/
│   │   ├── config/
│   │   ├── transport/http/           # 路由、middleware、请求响应 DTO
│   │   ├── auth/
│   │   ├── site/
│   │   ├── language/
│   │   ├── content/
│   │   ├── publication/
│   │   ├── lead/
│   │   ├── media/
│   │   ├── audit/
│   │   └── platform/                 # 数据库、存储、日志基础实现
│   ├── migrations/
│   ├── tests/integration/
│   └── go.mod
├── contracts/openapi.yaml            # 前后端接口契约
├── deploy/                           # Dockerfile、Compose、Nginx 配置
├── scripts/                          # 备份、恢复及初始化辅助脚本
├── CURRENT-SCOPE.md
└── ARCHITECTURE.md
```

`web` 上述结构使用多个根布局，实施时应迁移并移除现有顶层 `web/app/layout.tsx`，由各根布局引入公共样式并输出自己的 `html` 和 `body`。官网根布局位于 `[locale]` 内，从已校验的路由参数设置语言和方向；根地址单独跳转，预览使用受保护布局。语言切换必须验证文档语言实际更新，必要时执行整页导航，不能继续固定 `<html lang="en">` 或仅在浏览器挂载后补改语言。管理后台的布局、页面和构建配置全部放在 `admin/`，不再放入 `web/app`。

## 4. 官网前端架构

### 4.1 路由与页面组织

| 路径示例 | 页面 |
| --- | --- |
| `/` | 按已确认的语言选择策略跳转；未配置访客偏好时使用站点默认语言 |
| `/{locale}` | 首页 |
| `/{locale}/services`、`/{locale}/services/{id}` | 业务列表及详情 |
| `/{locale}/products`、`/{locale}/products/{id}` | 产品列表及详情 |
| `/{locale}/about` | 关于我们 |
| `/{locale}/news`、`/{locale}/news/{id}` | 新闻列表及详情 |
| `/{locale}/contact` | 联系我们及咨询表单 |
| `/{locale}/partners` | 合作介绍及申请表单 |
| `/{locale}/careers`、`/{locale}/careers/{id}` | 招聘列表及岗位详情 |

`locale` 使用后台配置的语言标识，例如 `zh-CN`、`en`、`es`。页面标题翻译与固定路由段分离，修改栏目名称不改变既有地址。详情使用稳定内容 ID，先保证地址稳定；以后如需本地化 slug，应同时设计唯一性及重定向规则。

- 语言标识在入口规范化并通过 Go 校验；未知或停用语言不查询公开正文。
- 关闭栏目、下架内容、逻辑删除内容均返回真实未找到状态，不能返回 200 的空页面。
- 启用语言中缺少对应译文时，采用未找到状态及该语言的返回栏目/首页引导；具体提示文案由站点配置。
- 后端不可用属于服务失败，返回错误状态和重试界面，不伪装为内容不存在。
- 列表的页码、分类等查询条件保存在 URL 中，刷新与分享时结果可复现。

### 4.2 组件与数据获取

页面布局、正文、列表和元信息在服务端读取已发布 DTO；菜单开合、轮播、语言选择及表单等交互使用客户端组件。公开官网的客户端组件不接收后台账号、草稿快照或数据库连接信息。官网内部的职责划分遵循 [Next.js Server and Client Components](https://nextjs.org/docs/app/getting-started/server-and-client-components) 的组件边界；独立后台的组件组织按其选定框架落实。

- `lib/api/server.ts` 使用内部 Go 地址，只允许服务端导入。
- `lib/api/browser.ts` 使用相对 `/api/v1` 地址，统一解析错误码、字段错误及请求编号。
- 同一页面的正文和 SEO 元信息应读取一致的发布版本；允许单次请求内复用读取结果。
- 数据请求设置超时和取消信号，避免后端故障耗尽前端连接。
- 默认按请求执行 SSR；动态语言列表和内容不在构建时固化，新增语言不要求重新构建网站。
- 日期、数字和金额按当前语言及明确时区格式化；币种来自产品数据，语言切换不触发换汇。

### 4.3 页面模块和主题

首页使用受控模块注册表，将 `banner`、`services`、`advantages`、`process`、`cooperation`、`consultation` 等类型映射到 React 组件。模块数据包含类型、稳定 ID、顺序、启用状态和经过校验的内容，不存储任意可执行代码。

后台只编辑受支持类型的字段；未知模块类型阻止发布，不能让前台运行时任意执行配置。可选模块为空时不显示空壳，关联内容由 Go 公开读取层过滤有效性。

品牌色、文字色和基础圆角等通过经过校验的 CSS 变量应用。企业文案、Logo、导航及联系方式来自发布配置；新增页面结构通过组件开发接入，不由 JSON 注入脚本或任意 CSS。

### 4.4 SEO、媒体和无障碍

- 每个页面生成当前语言的标题、描述、canonical 和可访问的语言替代地址。
- 站点地图只使用 Go 提供的有效发布清单，排除关闭栏目、停用语言和缺失译文。
- 后台及预览设置不收录策略，同时执行认证；`robots.txt` 不能替代权限检查。
- 图片提供尺寸、替代文本及适合视口的展示方式，避免布局跳动和移动端下载过大图片。
- 字体选择覆盖目标语言；首个站点含从右向左语言时，通过方向配置及布局验收落实。
- 输入框有标签，错误与字段关联，导航和弹窗支持键盘及焦点管理。

## 5. 管理后台前端架构

`admin/` 是独立前端应用，管理页面、路由、组件、依赖、构建脚本和测试均在该目录维护。当前仅确定目录与职责，后台框架和 UI 组件库在初始化时选择，不因官网使用 Next.js 就强制后台采用同一框架。

### 5.1 页面划分

| 路由区域 | 内容 |
| --- | --- |
| `/admin/login` | 登录 |
| `/admin/site` | 品牌、公共配置、导航及首页 |
| `/admin/languages` | 语言启停、默认语言、排序及界面翻译 |
| `/admin/content/{kind}` | 业务、产品、文章、岗位及固定页面内容列表 |
| `/admin/content/{kind}/{id}` | 语言选择、草稿编辑、预览及发布 |
| `/admin/leads/{type}` | 咨询、合作和应聘登记查询及导出 |
| `/admin/users` | 后台账号查看 |
| 预览操作 → `/preview/{id}/{locale}` | 从后台打开 web 的受保护页面，使用官网模板渲染草稿 |

后台浏览器直接调用 Go 管理接口。应用启动时通过 `/api/v1/admin/me` 读取当前用户和能力清单，路由守卫控制登录跳转和菜单，Go 对每个数据请求继续鉴权。静态页面壳可以被下载，但其中不能包含账号、草稿或留资；未认证时不加载受保护数据。401 返回登录入口，403 显示权限不足，不将二者混淆。

### 5.2 编辑状态

- 编辑器分别管理当前语言、服务器草稿版本、已加载内容和未保存修改。
- 切换语言或离开页面时提示未保存内容；失败时保留本地输入。
- 保存请求携带草稿版本，版本冲突返回 409，提示重新读取并人工合并。
- 当前发布记录和草稿分别展示，不能用“已保存”代替“已发布”。
- 预览按登录用户权限读取草稿，页面及接口使用 `no-store`；不生成无需认证的分享预览链接。
- 发布按钮显示目标语言和影响范围，提交时固定所选草稿版本，防止预览后他人修改被误发布。
- 导出通过受保护下载请求完成，浏览器收到文件后释放临时下载对象。

前台字段校验提升填写体验，服务端定义最终约束；接口返回稳定错误码及字段路径，前端按当前语言映射提示，不依赖 Go 返回中文句子作为业务判断依据。

### 5.3 独立后台与官网预览协作

- 后台先保存草稿，再使用稳定内容 ID、目标语言和草稿版本打开 `/preview/{id}/{locale}`，不在 URL 传递正文、密码或会话 token。
- 该路由由 `web` 提供：Next.js 将同源登录 cookie 转发到固定 Go 内部地址，Go 验证会话和内容读取权限后返回预览 DTO。
- 预览使用与官网一致的页面模块及样式，只有数据读取入口不同；草稿图片使用需认证的管理媒体地址。
- 若请求中的草稿版本与当前版本不一致，明确提示内容已更新，避免把新草稿误认为刚保存的版本。
- 预览不启用表单提交及其他真实业务写入，页面和接口禁止缓存并设置不收录标记。
- `admin` 与 `web` 通过地址及接口契约协作，无需将官网组件复制到后台，也不互相导入源代码。

### 5.4 构建和本地开发

- `web` 与 `admin` 分别维护依赖及锁文件，拥有独立的启动、构建、检查和测试命令。
- 后台默认产出静态客户端应用，资源基础路径和路由基础路径均为 `/admin/`；具体配置语法按选定框架落实。
- 本地联调使用同源代理，将 `/admin/` 转发到后台开发服务、官网及预览转发到 Next.js、接口转发到 Go，确保登录和预览沿用生产同源行为。
- 两个前端分别从同一 OpenAPI 契约获取类型；接口变更同时检查两个工程，不能只验证官网构建。

## 6. Go 后端架构

### 6.1 分层与依赖

每个业务模块按“HTTP 处理器 → 应用服务 → 数据访问接口 → MySQL / 存储实现”组织。应用服务管理业务规则和事务；HTTP 处理器负责解析、调用及响应，不直接拼接 SQL。

| 模块 | 职责 |
| --- | --- |
| `auth` | 密码验证、会话、角色能力及当前用户 |
| `site` | 栏目开关、品牌与公共配置、导航、首页配置 |
| `language` | 语言配置、默认语言约束和基础字典 |
| `content` | 内容类型、草稿、翻译、查询及关联校验 |
| `publication` | 快照生成、生效指向、上下架、删除和公开可见性 |
| `lead` | 三类表单、幂等提交、管理员查询和 Excel 导出 |
| `media` | 上传校验、存储、媒体引用及读取权限 |
| `audit` | 操作人、对象、时间、结果及请求编号 |

通用发布能力由 `publication` 提供，各业务类型通过显式校验器定义字段，不将全部内容处理成无约束的万能 JSON。

### 6.2 服务端执行原则

- Go `context` 贯穿请求、数据库和文件操作，传播取消与超时。
- 通过统一入口配置请求 ID、结构化日志、异常恢复、认证、授权、CSRF 和限流。
- 限制请求体、上传体积和分页大小，SQL 排序字段使用白名单。
- 参数使用占位符，时间统一以 UTC 存储，API 使用带时区的标准时间字符串。
- ID 在 API 中以字符串传输，避免浏览器对大整数的精度丢失。
- 发布、默认语言切换及留资幂等写入使用事务；事务内不执行外部 HTTP 请求或生成大文件。
- 优雅关闭时停止接受新请求并等待已有请求完成，再关闭连接池和文件资源。

## 7. 数据模型

### 7.1 核心关系

`内容实体 → 多语言草稿 → 不可变发布记录 → 当前生效指向` 是内容模型的核心。站点公用配置和可翻译配置也遵循相同发布原则，账号及运行设置单独管理。

| 逻辑表 | 关键字段 | 说明 |
| --- | --- | --- |
| `admin_users` | id、login_name、password_hash、role、status | 两级角色，登录名唯一 |
| `admin_sessions` | token_hash、user_id、expires_at、revoked_at、csrf_secret | 会话 token 只保存摘要，支持撤销 |
| `languages` | code、display_name、enabled、sort_order、direction | code 唯一且有内容后保持稳定 |
| `site_runtime` | id、default_locale、revision | 单行站点运行设置，默认语言外键指向 languages |
| `content_items` | id、kind、stable_key、deleted_at、created_at | 稳定内容身份，不存放直接可变的线上正文 |
| `content_drafts` | id、item_id、locale、payload、schema_version、revision、updated_by | 每项内容每种语言一份当前草稿 |
| `content_publications` | id、item_id、locale、draft_revision、snapshot、schema_version、published_by、published_at | 发布时生成完整且不可变的快照 |
| `publication_heads` | item_id、locale、publication_id、visibility、revision | 每项内容每语言一条当前生效指向及上下架状态 |
| `publication_refs` | publication_id、target_item_id、relation_type | 保存关联关系，运行时检查目标是否仍可公开访问 |
| `media_assets` | id、storage_key、mime_type、size、width、height、created_by | 存储元数据及不可变文件标识 |
| `media_refs` | asset_id、draft_id 或 publication_id | 发布资源保留和媒体公开可见性判断依据 |
| `leads` | id、type、locale、source_path、related_item_id、name、phone、email、payload、created_at | 共同字段可查询，类型专属字段经过对应结构校验 |
| `idempotency_records` | scope、key_hash、request_hash、result_id、expires_at | 表单及发布请求的重复提交控制 |
| `audit_logs` | actor_id、action、object_type、object_id、request_id、created_at、result | 必要操作追溯，不保存密码及完整留资正文 |

表名为建模依据，正式 DDL 放入迁移文件；草稿和发布记录中应拥有可被媒体引用的稳定记录 ID。跨表关联优先使用外键，并由服务层检查内容类型匹配及语言归属。

### 7.2 内容类型与结构化字段

- `kind` 使用受控枚举：service、product、article、job、page、category、site_config、ui_dictionary 等。
- `stable_key` 标识 home、about、contact、partners、careers 等固定页面，同类固定键唯一。
- 草稿及快照包含标题、摘要、SEO、正文或受控模块、关联项及展示属性；字段格式由对应类型校验器决定。
- 关系、筛选字段与 JSON 快照结合使用：需要排序、分类或查询的已发布字段，发布时同步写入相应发布记录的投影列或索引表。
- 公开列表只能从当前生效发布记录的投影筛选排序，不能读取草稿标题、分类、价格或草稿顺序。
- 产品金额使用精确小数及独立币种字段，API 可用字符串表达金额；面议和隐藏价格采用明确状态。
- 富文本保留编辑器所需结构，发布时校验并生成经过清理的渲染数据；禁止在前台执行正文中的任意脚本。

### 7.3 共用属性与多语言属性

图片、价格或排序等属性可从共用草稿复制到某语言草稿，但发布快照保存最终解析值。修改共用草稿不会悄悄改变已经发布的语言版本，运营人员应对受影响语言分别发布。

跨语言全局配置，如栏目总开关和统一模块顺序，使用独立的共用配置发布对象，内部使用保留的 `und` 标识。语言表保留对应系统记录以满足外键，但不允许其出现在访客语言列表、被启用或被设为默认语言。共用配置发布明确提示“影响所有语言”；语言导航文案及正文仍独立发布。

`languages` 和 `site_runtime` 属于运行设置，启停及默认语言切换在确认后立即生效，并写审计记录。基础 UI 字典是每语言的发布对象，启用语言前必须具备完整的有效字典和首页发布内容。

### 7.4 约束与索引

- 草稿唯一键：`(item_id, locale)`；生效指向唯一键同样为 `(item_id, locale)`。
- 发布记录唯一键：`(item_id, locale, draft_revision)`，配合幂等键避免同一草稿重复生成快照。
- 生效指向必须引用相同 item 和 locale 的发布记录，通过复合约束及事务校验保证。
- 留资按 `(type, created_at, id)` 建索引；常用列表根据实际查询建立类型、语言、可见性和排序索引。
- 只记录一个 `default_locale`，切换时锁定站点设置行；被设为默认的语言必须启用。
- 显示顺序相同采用稳定 ID 作为最后排序条件，避免分页重复或遗漏。
- 历史发布记录及其资源不随逻辑删除物理清理；保留期限和数据清理策略需另行确认。

## 8. 多语言实现

官网多语言统一使用 next-intl，负责翻译、语言上下文、带语言前缀的导航及格式化。当前页面骨架已在 `web/i18n/`、`web/messages/` 和 `web/app/[locale]/` 接入中文、英文、西班牙语；仅本阶段采用本地词典与静态语言配置，新增语言需要重新构建。下列后台驱动语言与免构建启用规则是业务阶段的目标，不代表当前已实现。接入 Go 后应替换词典与语言配置来源，并调整静态生成及入口路由策略。

1. Go 返回启用语言、默认语言和当前语言已发布的基础字典。
2. Next.js 从路径确定语言，将语言显式传入公开内容接口，不能依赖服务进程的全局当前语言变量。
3. 同一内容 ID 对应多个语言版本；API 返回可公开访问的译文清单，语言切换按 ID 构造对应地址。
4. 缺少正文译文时不回退成其他语言正文。提示与返回入口使用目标语言已发布的字典。
5. 未启用语言可录入、预览及发布，但所有公开读取仍被语言开关拦截。
6. 语言启用前检查字典必需键和首页内容；新增功能引入新文案键时，应同步维护字典结构版本和现有语言完整性。
7. 日期、数字格式化使用语言标识；时区由站点配置确定，不将语言当作时区。

字段翻译和 UI 翻译均来自后台数据，不把语言列表写成代码中的固定联合类型，不为每新增语言增加数据库列。启用第三种语言无需修改路由代码或重新构建。

## 9. 草稿、发布与公开读取

### 9.1 保存草稿和并发控制

读取草稿时返回 `revision`。保存请求携带 `expectedRevision`，服务端执行带版本条件的更新；更新成功后 revision 加一，未匹配则返回 409，不覆盖其他人的修改。

发布请求携带选中的 `expectedDraftRevision` 和幂等键。发布服务重新校验草稿版本，不能把其他人刚保存的新内容当作用户刚预览的内容发布。

### 9.2 发布事务

1. 检查当前用户的发布能力，锁定内容与对应草稿/生效指向，校验未删除状态。
2. 检查目标语言存在、必填字段完整、媒体有效及关联对象正确；语言未启用本身不阻止发布。
3. 生成包含最终文案、展示属性、媒体引用及结构版本的发布快照。
4. 写入不可变发布记录、列表查询投影和引用关系。
5. 更新当前生效指向及可见状态，记录操作日志及幂等结果。
6. 提交事务后返回发布 ID；任何事务步骤失败均不改变原生效记录。

数据库事务用 `sql.Tx` 管理，事务内所有 SQL 使用同一事务对象，避免混用事务外连接。参考 [Go 数据库事务说明](https://go.dev/doc/database/execute-transactions)。

重新上架现有内容只改变其生效可见状态，并记录操作，不修改历史快照。重新发布同一草稿版本复用已有快照；新的正文修改必须先保存为新草稿版本。

### 9.3 公开读取条件

Go 公开读取层统一检查：语言已启用、栏目已开启、内容未删除、当前语言生效指向可见且发布记录有效。列表、详情、站点地图、关联卡片及媒体可见性共用相同规则。

共用配置 `und` 仅由服务端内部读取后组合为目标语言的公开站点 DTO，不作为可直接查询的语言。通用公开内容接口仅开放访客可见的业务类型，不允许查询内部共用配置或任意历史记录；共用 Logo 等媒体按实际公开配置引用关系判断访问权限。

下架修改对应语言的可见状态；栏目关闭更新共用配置；逻辑删除标记内容实体。旧记录保留，但不能通过指定旧发布 ID 从公开接口读取。

### 9.4 缓存基线

初期采用“动态业务数据按请求读取，构建静态资源缓存”的基线，以满足单机及发布可见性要求：

- 公开内容、语言配置、栏目开关和站点地图不启用跨请求业务缓存，显式按当前 Next.js 版本配置动态读取。
- Nginx 和 CDN 不缓存业务 HTML、RSC 响应及 JSON 接口，不使用全站强制缓存规则。
- 后台、草稿预览、表单和导出统一 `no-store`；浏览器导航与刷新也需验证不复用过期业务页面。
- 带构建哈希的 JS、CSS 和字体可长期缓存；受内容可见性约束的上传图片走媒体权限规则。
- 基线下发布提交后，下一次新的公开读取即使用新生效版本；已打开页面不要求主动推送更新。

本基线没有需要主动清理的业务跨请求缓存，因此不额外部署消息队列或缓存失效服务。今后如因实际负载启用 ISR、跨请求缓存或 CDN 页面缓存，必须先补齐发布事件持久化、失效重试、下架同步、缓存键语言隔离及故障反馈，不能只添加 TTL 就宣称满足发布规则。Next.js 自托管和缓存能力以 [官方自托管说明](https://nextjs.org/docs/app/guides/self-hosting) 及项目安装版本文档为准。

## 10. 前后端接口契约

### 10.1 路由分组

| 方法与路径示例 | 作用 | 权限 |
| --- | --- | --- |
| `GET /api/v1/public/site?locale=es` | 当前公开品牌、导航、模块及界面字典 | 公开，只返回生效数据 |
| `GET /api/v1/public/languages` | 启用语言及默认语言 | 公开 |
| `GET /api/v1/public/content/{kind}?locale=es&page=1` | 已发布列表 | 公开 |
| `GET /api/v1/public/content/{kind}/{id}?locale=es` | 已发布详情及有效译文清单 | 公开 |
| `POST /api/v1/public/leads/{type}` | 咨询、合作、应聘登记 | 公开，服务端校验及限流 |
| `POST /api/v1/admin/auth/login`、`logout` | 登录及退出 | 登录入口限流，退出需要认证和 CSRF 校验 |
| `GET /api/v1/admin/me` | 当前账号及能力清单 | 登录用户 |
| `GET /api/v1/admin/content/{kind}` | 内容管理列表 | 内容维护能力 |
| `PUT /api/v1/admin/content/{id}/drafts/{locale}` | 保存指定语言草稿 | 内容维护能力 |
| `GET /api/v1/admin/content/{id}/drafts/{locale}` | 编辑及预览数据 | 内容维护能力 |
| `POST /api/v1/admin/content/{id}/publications/{locale}` | 发布指定草稿版本 | 发布能力 |
| `POST /api/v1/admin/content/{id}/unpublish/{locale}` | 下架对应语言 | 下架能力 |
| `DELETE /api/v1/admin/content/{id}` | 逻辑删除，影响全部语言 | 删除能力，确认影响范围 |
| `GET /api/v1/admin/leads/{type}`、`/{id}` | 留资列表及详情 | 仅管理员 |
| `GET /api/v1/admin/leads/{type}/export` | 导出筛选结果 | 仅管理员，受导出条数限制 |
| `POST /api/v1/admin/media` | 上传媒体 | 内容维护能力 |
| `GET /media/{assetId}` | 读取可公开引用的媒体 | Go 检查当前公开引用 |
| `GET /api/v1/admin/media/{assetId}` | 草稿和保留媒体读取 | 相应内容访问权限 |

此表是分组与行为约定，完整的创建、语言设置、字典、媒体及账号接口在 OpenAPI 中定义。`kind` 和 `type` 为服务端枚举，不允许直接映射任意表名。

### 10.2 请求与响应约定

- API 使用 JSON 和 UTF-8，导出及媒体响应使用对应文件类型。
- 列表返回 `items`、`page`、`pageSize`、`total`，分页参数设置服务端上限。
- 成功返回 `data` 和 `requestId`；错误返回 `error.code`、`error.message`、可选 `error.fields` 和 `requestId`。
- 400 用于请求结构错误，401 用于未登录，403 用于权限不足，404 用于资源不可见，409 用于版本或幂等冲突，422 用于字段校验，429 用于限流，5xx 用于服务故障。
- 未提交草稿版本等必要参数应明确拒绝；时间、ID、金额的格式写入接口 Schema。
- 发布和表单使用 `Idempotency-Key`，服务端同时校验请求内容摘要，不能将相同键对应的不同请求直接当作成功。
- 下载响应设置文件名、正确类型和禁止缓存，错误时返回正常错误响应而不是伪装成损坏的 Excel。

OpenAPI 是接口字段的共同依据；Go 请求/响应结构及前端 TypeScript 类型需保持一致。类型可从契约生成，但生成的类型不替代运行时校验。

## 11. 认证、权限与安全边界

### 11.1 登录和会话

- 使用服务端会话方案，Go 生成高熵随机 token，数据库仅保存摘要；登录成功重新生成会话，退出时撤销。
- 生产 cookie 使用 `HttpOnly`、`Secure`、`SameSite=Lax`、`Path=/`，不设置跨域 Domain；可采用 `__Host-` 前缀。
- 会话有效期和闲置时限配置化，每次请求检查用户启用状态及当前权限；角色调整不依赖旧 token 中的固定权限。
- 密码使用专用自适应密码哈希库保存，具体算法与参数在认证模块实现时锁定；初始化密码通过受保护输入传入，不保留固定默认密码。
- 管理端修改操作验证同源 Origin 和会话绑定 CSRF token；登录也验证来源并限流。`SameSite` 不能作为唯一 CSRF 防线。
- Next.js 服务端预览请求只向配置好的 Go 内部地址转发必要 cookie，不向用户可控地址转发认证信息。

### 11.2 两级角色

管理员拥有站点运行配置、账号查看、内容操作和留资访问能力。内容编辑拥有授权范围内的内容读取、草稿保存、预览及媒体上传能力。

编辑是否可发布和上下架仍是需求待确认项：代码用显式能力映射表达，未配置时默认拒绝，管理员可执行。前端按 Go 返回的能力清单展示操作；具体业务实体与语言范围继续由 Go 验证。

每个留资详情、列表和下载入口都要求管理员身份，包括可猜测 ID 的直接请求。错误响应不包含数据片段、文件路径或下载令牌。

### 11.3 输入和运行边界

- 富文本采用白名单清理，外部 URL 限定协议，服务端不自动抓取任意用户输入地址。
- 上传检测实际格式及大小，初期允许经过验证的栅格图片；SVG 等主动内容格式需专门清理方案后才开放。
- CORS 默认不开放跨域凭据；开发环境通过同源代理访问 Go，避免生产中放宽来源限制。
- 代理层和 Go 配置可信代理地址，不能直接信任任意 `X-Forwarded-For` 作为限流依据。
- 日志和监控不记录认证 token、密码、完整表单正文或数据库连接秘密。

## 12. 留资、幂等与 Excel 导出

### 12.1 提交及查询

三类留资在同一逻辑表中通过 `type` 区分，使用各自校验结构和接口入口。Go 校验对应栏目是否开放、岗位是否仍招聘、关联内容是否有效，不能只相信浏览器带来的 ID。

浏览器为一次提交生成随机幂等键并在重试时复用。Go 在事务内创建幂等记录和留资：相同键、相同内容返回原提交结果，不新增数据；相同键、不同内容返回 409。并发请求依靠唯一约束串行判定，事务失败可安全重试。

公开提交接口只返回提交编号和结果，不返回完整留资详情。幂等记录保存期限、匿名请求作用域和允许重试窗口在接口契约中明确；过期后不承诺仍然去重。

基础限流以单 Go 实例为前提，内存计数配合代理层限制；进程重启会重置内存计数。若扩为多副本，应增加共享计数实现，不能把单实例限制当作全局限制。

### 12.2 导出

- 管理员导出沿用列表筛选条件，时间区间统一采用左闭右开并转换为 UTC 查询。
- 在限定规模内同步生成 `.xlsx` 并通过受保护响应下载，配置最大条数、执行超时和临时文件大小。
- 导出数据采用稳定排序和一致读取策略，避免生成过程中新增记录导致重复或跳行。
- 手机、ID 等列写为文本，用户内容一律作为数据写入，不调用公式写入接口。
- 请求取消或失败后清理临时文件；临时目录不在公开媒体路径内。
- 记录操作者、类型、导出时间、条数和结果，不把完整导出内容写日志。
- 需要更大规模异步导出时，先补充任务持久化、下载授权、有效期和清理机制，再扩展接口。

## 13. 媒体存储与访问

存储接口提供写入、读取及删除能力，业务只引用 asset ID，不把磁盘绝对路径或存储桶凭据返回前端。

1. 上传在临时目录完成格式、尺寸、大小校验，通过后保存为随机文件名并登记媒体记录。
2. 同名上传生成新 asset，不覆盖既有文件，保证旧发布记录引用内容稳定。
3. 草稿及发布分别登记引用；发布记录中的图片必须在事务确认时存在。
4. `/media/{assetId}` 仅对存在当前可公开引用的媒体放行。历史记录保留文件，但只有历史引用并不足以使媒体公开。
5. 管理员和有内容访问权限的编辑通过管理接口读取草稿媒体；公开接口不得因文件 ID 已知而跳过检查。
6. 被草稿或历史发布记录引用的文件不物理删除；无引用临时文件的清理周期在部署时明确。

初期不将上传目录作为 Nginx 无条件静态目录，也不将需要撤销访问的图片放入长期公开 CDN 缓存。此类图片使用可校验的媒体地址及禁止缓存策略；前端不经由会绕过后续权限检查的持久图片优化缓存。已下载到访客设备的文件无法撤回，这与后续请求是否被允许访问分开处理。

如后续启用对象存储或图片 CDN，应保留相同的可见性规则，明确签名期限、缓存撤销和资源发布策略。

## 14. 部署、配置与备份

### 14.1 单机部署

初期运行中的 Compose 服务为 `nginx`、`web`、`api`、`mysql`；`admin` 独立构建为静态产物并打包到 Nginx 镜像中，因此无需独立的后台 Node 进程。若最终选定框架需要服务端运行，再增加内部 `admin` 服务。仅 Nginx 暴露 80/443，其余使用内部网络。数据库、媒体和备份使用明确的持久化位置，容器重建不得丢失数据。

`web` 的 Next.js 采用 Node 运行服务，承担官网 SSR 和受保护的草稿预览。`admin` 的静态构建方式与官网运行方式分别配置。两个前端的生产构建在开发机或 CI 完成，避免在 2C4G 业务机上同时构建、运行和备份造成资源竞争。

Nginx 将 `/admin` 规范化为 `/admin/`，后台页面深链接回退到后台入口文件；`/admin/` 下不存在的 JS、CSS 等资源返回 404，不能回退成 HTML。`/api/v1/`、`/media/` 和 `/preview/` 不参与后台页面回退。后台入口 HTML 禁止缓存，带内容哈希的静态资源可以缓存，所有用户数据仍通过 Go 鉴权接口获取。

Nginx 保留请求编号、协议及必要代理信息，配置请求体上限、超时及限流。若使用 Cloudflare，需单独核验代理来源、HTTPS 和缓存配置，不假定接入即具备国内外一致的访问效果。

### 14.2 环境配置

| 配置 | 使用位置 | 说明 |
| --- | --- | --- |
| `SITE_ORIGIN` | Next.js / Go | 生成 canonical、校验请求来源，仅接受配置的站点地址 |
| `GO_API_INTERNAL_URL` | Next.js 服务端 | 内部 Go 地址，不向浏览器暴露 |
| `HTTP_ADDR` | Go | 内部监听地址 |
| `DATABASE_DSN` | Go / 迁移命令 | 数据库连接秘密，通过部署环境注入 |
| `MEDIA_ROOT` | Go | 私有上传持久化目录 |
| `SESSION_TTL` | Go | 会话时限，单位在配置规范中固定 |
| `UPLOAD_MAX_BYTES` | Go / 代理 | 文件体积限制，双方一致 |
| `EXPORT_MAX_ROWS` | Go | 同步导出上限 |
| `LOG_LEVEL` | Go / Next.js | 日志级别，不改变脱敏规则 |

变量名是目标约定，实际默认值在 `.env.example` 和部署文档中记录；示例文件只使用占位值。数据库凭据等绝不能使用面向浏览器暴露的环境变量前缀。企业名称、栏目和翻译在后台维护，不塞入部署变量。

### 14.3 初始化与更新

1. 固定代码和依赖版本，分别构建 `web`、`admin` 产物与 Go 二进制/镜像。
2. 备份现有数据库和媒体，在单次维护步骤中运行版本化迁移。
3. 初始化站点设置、语言及管理员；重复初始化不能覆盖已有企业内容。
4. 启动服务，检查数据库就绪、官网首页、后台登录、后台深链接刷新、预览及表单链路。
5. 将示例数据作为显式选择，仅向新建或专用测试数据库导入。

迁移由单独命令执行，避免多个服务实例启动时同时修改表结构。Schema 采用兼容性变更并保留升级顺序；应用回退前检查数据库兼容性，不能盲目执行破坏性反向迁移。

### 14.4 备份与恢复

- 备份包含 MySQL、媒体、必要配置及迁移版本，数据库快照和文件副本应属于可对应的恢复批次。
- 文件采用不可变保存，可先做数据库一致性备份，再复制完整媒体集合；备份期间暂停物理清理，确保快照引用文件未被移除。
- 手动方式提供可复用步骤，自动定时方式作为部署选项；保留周期和异地副本位置由具体项目确定。
- 备份使用访问控制及适合的加密措施，密钥与备份分开保存。
- 在独立环境验证恢复，检查公开内容、历史记录、媒体和测试留资；恢复环境撤销旧后台会话。
- Excel 导出仅作为业务数据输出，不用于替代系统备份。

## 15. 可观测性、测试与交付验证

### 15.1 运行检查

- Go 提供存活和就绪检查，就绪检查包含必要数据库连接；接口不返回连接秘密和详细内部拓扑。
- 日志包含服务、级别、时间、请求编号、耗时和结果，前后端请求编号可关联。
- 关注接口错误率、响应时间、数据库连接池、磁盘空间、上传失败、导出失败及备份结果。
- 基础业务日志与审计日志分开，避免调低日志级别后丢失必要发布和导出记录。

### 15.2 测试分工

| 层级 | 验证重点 | 对应需求验收 |
| --- | --- | --- |
| Go 单元测试 | 角色能力、类型字段校验、可见性、语言约束 | AC-06、AC-08、AC-11 |
| MySQL 集成测试 | 发布原子性、乐观锁、幂等并发、唯一约束 | AC-04、AC-05、AC-10、AC-13 |
| API 契约测试 | 字段格式、状态码、分页、错误和权限 | 内容与留资接口约定 |
| 官网组件测试（web） | 语言切换、表单字段错误、页面模块及交互状态 | AC-07、AC-14 |
| 后台组件测试（admin） | 编辑未保存提示、版本冲突、能力控制、查询及导出反馈 | AC-11、AC-13 |
| 浏览器端到端测试 | 至少三种语言、发布前后、关闭栏目、管理员导出 | AC-01 至 AC-12 的关键流程 |
| 部署验证 | SSR、元信息、真实 HTTP 状态、媒体权限及恢复 | AC-15、AC-16 |

测试应覆盖失败路径：公开接口请求草稿、编辑请求留资、重复提交、同时保存、停用默认语言和后端不可用。数据库事务及并发测试使用真实 MySQL，不能仅用 mock 推断事务正确性。

当前 `web` 已有 `dev`、`build`、`start`、`lint` 脚本；`admin` 尚未初始化脚本，需在技术栈确定后补充。联调需额外验证后台深链接刷新、同源代理、跨工程预览及会话失效。本文档更新不意味着上述功能或测试已经完成。

## 16. 待落实的技术与业务决策

| 事项 | 当前处理方向 | 落实时点 |
| --- | --- | --- |
| 后台技术栈 | `admin` 独立前端工程，框架和组件库待选，默认按静态客户端应用设计 | 后台初始化前 |
| 编辑发布权限 | 显式能力映射，确认前默认不授予 | 认证及后台实现前 |
| 账号维护操作 | 先实现初始化和查看，新增、停用、重置范围按需求确定 | 账号模块开发前 |
| 启用语言及方向 | 数据驱动，至少三种语言验证；RTL 按目标语言落实 | 前台布局和字典设计前 |
| 首次语言选择 | 默认站点语言；访客偏好或浏览器协商需确定具体优先级 | 路由实现前 |
| 缺失译文 | 本文采用 404 和目标语言返回引导作为技术默认方案 | 与需求方确认后更新范围文档 |
| 表单及附件 | 使用预设类型结构，字段与简历附件按待确认项处理 | 留资模块开发前 |
| 依赖和工具 | UI 编辑器、MySQL 驱动、Excel 库、迁移及测试工具锁定版本 | 对应模块初始化时 |
| 导出和上传限额 | 同步处理、配置化上限，以测试验证具体数值 | 接口联调前 |
| 性能和缓存 | 动态 SSR 基线，压测后决定是否增加缓存 | 部署验收前 |
| 备份及运行环境 | 单机参考部署，具体地区、规格、保留周期按项目确定 | 上线前 |

涉及架构方向变化时，同步修改本文件及需求范围；若建立 `DECISIONS.md`，记录选择原因及替代方案。接口明细、字段长度、DDL 和具体部署命令分别进入 OpenAPI、迁移文件和部署文档，避免在多个文档中维护互相冲突的定义。
